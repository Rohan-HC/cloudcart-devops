apiVersion: v1
kind: Service
metadata:
  name: user-db
  namespace: cloudcart
  labels:
    app.kubernetes.io/name: user-db
    app.kubernetes.io/part-of: cloudcart
spec:
  clusterIP: None
  selector:
    app.kubernetes.io/name: user-db
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
