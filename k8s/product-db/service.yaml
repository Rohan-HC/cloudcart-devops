apiVersion: v1
kind: Service
metadata:
  name: product-db
  namespace: cloudcart
  labels:
    app.kubernetes.io/name: product-db
    app.kubernetes.io/part-of: cloudcart
spec:
  clusterIP: None
  selector:
    app.kubernetes.io/name: product-db
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
