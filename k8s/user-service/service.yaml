apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: cloudcart
  labels:
    app.kubernetes.io/name: user-service
    app.kubernetes.io/part-of: cloudcart
spec:
  type: ClusterIP

  selector:
    app.kubernetes.io/name: user-service

  ports:
    - name: http
      port: 8081
      targetPort: http
